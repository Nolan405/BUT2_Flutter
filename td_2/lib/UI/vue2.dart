import 'package:flutter/material.dart';

class Vue2 extends StatelessWidget {
  const Vue2({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("Vue 2")
    );
  }
}