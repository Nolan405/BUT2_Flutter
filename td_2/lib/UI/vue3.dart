import 'package:flutter/material.dart';

class Vue3 extends StatelessWidget {
  const Vue3({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("Vue 3")
    );
  }
}