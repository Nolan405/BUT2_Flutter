import 'package:flutter/material.dart';

class Vue1 extends StatelessWidget {
  const Vue1({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Vue 1")
    );
  }
}